import {ChangeDetectionStrategy, Component, OnInit} from '@angular/core';

@Component({
    selector: 'app-helpers-size',
    templateUrl: './helpers-size.component.html',
    changeDetection: ChangeDetectionStrategy.OnPush
})
export class HelpersSizeComponent implements OnInit {

    constructor() {
    }

    ngOnInit() {
    }
}
